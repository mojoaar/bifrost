node_modules/
.next/
data/
.env
.env.local
.env.*.local
*.db
*.db-journal
.DS_Store
dist/
coverage/
.turbo/
*.tsbuildinfo
.superpowers/

# User content is its own git repo (see docs/bifrost/git.md) and holds local
# blog data. The app recreates these directories on startup, so never track it.
# Anchored to the repo root so it does not also match lib/content/.
/content/
